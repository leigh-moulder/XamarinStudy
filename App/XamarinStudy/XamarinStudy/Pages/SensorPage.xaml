<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinStudy.Pages.SensorPage"
             Title="Sensors">

    <ContentPage.Resources>
    </ContentPage.Resources>

    <NavigationPage.TitleView>
        <Label Text="Sensors" TextColor="White" FontSize="Title"/>
    </NavigationPage.TitleView>
    
    <ContentPage.Content>
        <StackLayout>
            <Grid Padding="2, 2, 2, 2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Text="Position:" VerticalOptions="CenterAndExpand"/>
                <Label Grid.Row="0" Grid.Column="1" x:Name="LabelPosition" Text="{Binding Position}"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>