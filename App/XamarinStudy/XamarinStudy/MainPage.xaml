<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinStudy.MainPage">

    <StackLayout Margin="5,5,5,5">
        <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
            <Label Text="Contacts" HorizontalTextAlignment="Start" TextColor="White" FontSize="Title"/>
        </Frame>
        
        <ListView
            x:Name="FavoriteContactsListView" 
            SeparatorVisibility="Default"
            SelectionMode="None"
            ItemsSource="{Binding FavoriteContacts}">
            
            <ListView.Header>
                <StackLayout Orientation="Horizontal">
                    <Label Text="Favorites"/>
                </StackLayout>
            </ListView.Header>
        </ListView>


        <ListView
            x:Name="ContactsListView" 
            SeparatorVisibility="Default"
            SelectionMode="None"
            ItemsSource="{Binding Contacts}">

            <ListView.Header>
                <StackLayout Orientation="Horizontal">
                    <Label Text="Contacts"/>
                </StackLayout>
            </ListView.Header>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding FirstName}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackLayout>
</ContentPage>
